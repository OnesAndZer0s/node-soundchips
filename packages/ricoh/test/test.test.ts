import { assert } from "chai";
import { RICOH } from "../src/index";




describe( "Calculator Tests", () => {
  it( "should return 5 when 2 is added to 3", () => {
    const result = RICOH;


    assert.equal( result, "RICOH" );

  } );
} );