import { assert } from "chai";
import { POKEY } from "../src/index";




describe( "Calculator Tests", () => {
  it( "should return 5 when 2 is added to 3", () => {
    const result = POKEY;


    assert.equal( result, "POKEY" );

  } );
} );